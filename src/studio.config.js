/**
 * Default Studio Laravel API base.
 *
 * Local default: `/api/v1` — Vite proxies `/api` to http://127.0.0.1:8080.
 * Prefer `.env` `VITE_TL_API_BASE` for staging/main/prod overrides.
 * See README.md "API target".
 */
export const STUDIO_API_BASE = '/api/v1';
